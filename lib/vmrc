# These have to be set to something somewhere, i.e. if they
# are supposed to be used but not set, vm will die.

VM_CONFIG=.vmrc
VM_FMT=qcow2

# That means you can kick these out if you don't want to use snapshots 

VM_SNAPSHOT_SEEDNAME_DEFAULT=seed
VM_SNAPSHOT_BASENAME_DEFAULT=base
VM_SNAPSHOT_BACKINGNAME_DEFAULT=backing

# You can override builtin defaults here, too.

VM_DATEFMT="%F %T"

# Or provide your own:

#VM_QEMU_DISPLAY=curses
